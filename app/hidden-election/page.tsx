'use client';

import { Container } from '@/components/common/container';
import { HiddenElectionContent } from './content';

export default function GeneralMeetingHiddenElectionPage() {
  return (
    <Container>
      <HiddenElectionContent />
    </Container>
  );
}
